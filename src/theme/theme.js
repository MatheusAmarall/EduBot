import { createTheme } from '@mui/material';

export const theme = createTheme({
  palette: {
    primary: {
      main: '#5B71EE',
    },
  },
  typography: {
    fontFamily: 'Poppins',
  },
});

export default theme;